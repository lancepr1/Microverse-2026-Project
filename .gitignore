__pycache__/
*.pyc
.venv/
venv/
.pytest_cache/

# Blender autosaves and build artifacts
*.blend1
*.blend2

# run artifacts from the file bus
runs/

# pipeline-generated outputs -- regenerated fresh by every run of
# scripts/run_microverse.py, not source
data/combined/
lanes/leiva_verification/outputs/
lanes/marchisano_attacks/outputs/

# dashboard local history db (runtime-generated)
lanes/mccray_dashboard/dashboard/*.db

# datasets never go in git (see data/README.md)
# CHANGED: was data/nlr/ + data/enf/ -- doesn't match the current data
# layout, scripts/run_microverse.py and main_run.py now both read from
# data/rawdata/ (00_raw_datasets/, "ENF-ML (CNN+MAMBA)"/, and the
# .blend file all live there together). Left the old two lines out
# entirely rather than keeping them alongside the new one -- they don't
# match anything real anymore and would just be confusing dead entries.
data/rawdata/
*.csv
*.parquet
*.zip
*.log
